{
  "name": "Groceries Plugin Dev Environment",
  "build": {
    "dockerfile": "Dockerfile"
  },
  
  // This runs AFTER the repository files are mounted into the container
  "postCreateCommand": "cd claude_project && unzip -o uigen.zip && npm install && npm run setup"
}