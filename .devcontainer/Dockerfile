# Use a standard Node/Python base image appropriate for your project
FROM mcr.microsoft.com/devcontainers/universal:2

# Install Claude Code globally during the image build phase
RUN npm install -g @anthropic-ai/claude-code